[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

# SPA: serve index.html for all routes (API is under /.netlify/functions/)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
